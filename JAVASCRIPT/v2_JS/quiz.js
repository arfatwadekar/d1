// Create a variable of type strings and try to add a number to it
let a = "1234";
console.log(a);

// 2. use type of operator to find the data type of the string in last question
console.log(typeof a);

// 3. create a  const object in js can you change it to hold a number later?
const number = {
  name: "arfat",
  age: 2,
  rollNumber: 20,
};
console.log(number);
number.name = "rahul";
console.log(number);

// try to add aner key to the upper object
number.lastname = "wadekar";
console.log(number);

// write a js prgm to create a word meaning dictinort of 5 words

const wordDictionary = {
  apple: "A fruit that grows on trees and is typically red, green, or yellow.",
  cat: "A small domesticated carnivorous mammal with soft fur, a short snout, and retractile claws.",
  computer: "An electronic device that can store, retrieve, and process data.",
  book: "A written or printed work consisting of pages glued or sewn together along one side and bound in covers.",
  sun: "The star around which the earth orbits, from which it receives light and heat.",
};

console.log(wordDictionary.apple);
console.log(wordDictionary.cat);
console.log(wordDictionary.computer);
console.log(wordDictionary.book);
console.log(wordDictionary.sun);
